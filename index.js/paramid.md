#paramid
-1
```
for (let a = 5; a >= 1; a--) {
    let str = ""
    for (let b = 1; b <= a; b++) {
        str = str + "*"
    }
    console.log(str);
}
```
-2
```
for (let a = 1; a <= 5; a++) {
    let = str = ""
    for (let b = 1; b <= a; b++) {
        str = str + "*"
    }
    console.log(str);
}
```
-3
```
for(let a=1;a<=5;a++){
    let =str=""
    for(let b=1;b<=a;b++){
        str=str+b
    }
    console.log(str);
}
```
-4
```
for(let c=5;c>=1;c--){
    let str=""
    for(let d=1;d<=c;d++){
        str =str+d
    }
    console.log(str);
}
```
-5
```
for (let a = 1; a <= 5; a++) {
    for (let b = 1; b <= 5; b++) {
        document.write("$");
    }
    document.write("<br>");
}
```
-6
```
for (let a = 1; a <= 10; a++) {
    let str = ""
    for (let b = 10; b >= a; b--) {
        str = str + b
    }
    console.log(str);
}
```
-7
```
for (let a = 1; a <= 5; a++) {
    let str = ""
    for (let b = 1; b <= a; b++) {
        str = str + "*"
    }
    console.log(str);
}
for (let a = 4; a >= 1; a--) {
    let str = ""
    for (let b = 1; b <= a; b++) {
        str = str + "*"
    }
    console.log(str);
}
```